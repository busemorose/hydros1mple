% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_safran.R
\name{get_safran}
\alias{get_safran}
\title{Download and aggregate SAFRAN-ISBA climate data over a catchment}
\usage{
get_safran(
  ctm_path,
  safran_path = NULL,
  maille_path = NULL,
  epsg = 2154,
  show_map = TRUE
)
}
\arguments{
\item{ctm_path}{Character. Path to the catchment shapefile (.shp or any format readable by \code{sf}).}

\item{safran_path}{Character. Path to the SAFRAN grid GeoPackage (.gpkg). Default: \code{"data/safran.gpkg"}.}

\item{maille_path}{Character. Path to the SAFRAN cell centroids KML file. Default: \code{"data/SIM2.kml"}.}

\item{epsg}{Integer. EPSG code for the projection used during spatial intersection. Default: \code{2154} (Lambert 93).}

\item{show_map}{Logical. If \code{TRUE}, displays a map of the intersected SAFRAN cells and the catchment. Default: \code{FALSE}.}
}
\value{
A \code{data.table} with one row per day and one column per SAFRAN-ISBA
parameter, containing the area-weighted aggregated values over the catchment.
}
\description{
Downloads daily SAFRAN-ISBA data from the GéoSAS EDR API for all grid cells
intersecting a catchment shapefile, then computes a weighted sum of all
meteorological parameters proportional to each cell's area within the catchment.
}
\examples{
\dontrun{
df <- get_safran(
  ctm_path   = "my_catchment.shp"
)
}

}
